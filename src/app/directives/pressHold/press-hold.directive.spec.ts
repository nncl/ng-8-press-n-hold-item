import { PressHoldDirective } from './press-hold.directive';

describe('PressHoldDirective', () => {
  it('should create an instance', () => {
    const directive = new PressHoldDirective();
    expect(directive).toBeTruthy();
  });
});
